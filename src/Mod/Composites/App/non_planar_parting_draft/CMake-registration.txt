# CMake registration draft for the non-planar parting solver.
#
# DRAFT — not yet appended to the real CMakeLists. Two registration sites:
#
# 1. nextdrape library (src/3rdParty/nextdrape/CMakeLists.txt) — add the
#    solver source to nextdrape_core so the OCCT traversal lives with the
#    rest of the C++ solver code:
#
#       add_library(nextdrape_core
#           src/DrapeEngine.cpp
#           ...
#           src/NonPlanarPartingSolver.cpp     # <-- add
#       )
#
#    The header goes in include/nextdrape/ (nextdrape convention) so the
#    binding includes <nextdrape/NonPlanarPartingSolver.hpp>.
#
# 2. Composites binding (src/Mod/Composites/CMakeLists.txt) — add the
#    pybind binding alongside CompositesDrape.cpp:
#
#       set(COMPOSITESTEST_SRCS
#           App/CompositesDrape.cpp
#           App/CompositesParting.cpp          # <-- add
#           ...
#       )
#
#    The binding produces a CompositesParting Python module (import
#    CompositesParting), mirroring CompositesDrape.
#
# Source locations when wired (move out of this _draft dir):
#   src/3rdParty/nextdrape/src/NonPlanarPartingSolver.cpp
#   src/3rdParty/nextdrape/include/nextdrape/NonPlanarPartingSolver.hpp
#   src/Mod/Composites/App/CompositesParting.cpp
