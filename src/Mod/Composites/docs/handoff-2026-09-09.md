# Handoff: StiffenerCompositeShell + solver overhaul session

**Date:** 2026-09-09 · **Branch:** `fem-unified` (FreeCAD `jwharington/FreeCAD`), nextdrape `master`
**Head at handoff:** FreeCAD `b053ee7794`, nextdrape `ad33fdd` — all pushed.
**This document:** session handoff, start here; durable records live in the
PRDs (`seam_composite_laminate.md`, `stiffener_composite_shell.md`),
`CONTEXT.md`, `adr/`, and `known-issues.md`.

## Session outcome (all committed & pushed)

Six threads, all complete:

1. **StiffenerCompositeShell implemented** (PRD §12 steps 1–4) — the
   stiffener's composite mode: web/foot split, combined joint layup,
   weave exclusivity, graceful degradation, example, 31 tests.
2. **TransferRosette closed-form frame solve** — replaced the ~40-drape
   bisection; suites went from minutes to ~1 minute.
3. **Known-issue #6 resolved** — the "stale Shape snapshot" was actually
   placement stripping in the Shape mirror.
4. **Known-issue #5 resolved** — weave shader re-attaches on document
   restore.
5. **Known-issue #7 resolved** — rosette-less fallback seed uses a true
   nearest-point projection.
6. **Known-issue #9 (code parts) resolved** — DAG dependency cycles
   broken via `App::PropertyLinkHidden`; datum touches purged; SCL resin
   set before references. Also **#4 verified + pinned** and the
   **shader-asset build gap** fixed (the black-weave root cause).

## StiffenerCompositeShell — what landed

- `StiffenerFP` gains `Laminate`/`Rosette` (+ hidden `SupportBase`).
  No `Laminate` = geometry-only (unchanged); `Laminate` = full composite.
- `make_stiffener` returns a `StiffenerSweep` dataclass: `shell`,
  `remainders`, `foot_faces`, `web_faces`, `foot_width`, `web_height` —
  provenance from the profile's loci keys, no geometric matching.
- `features/StiffenerCompositeShell.py`: the flow. Web shell (stiffener's
  own laminate + auto-created `_WebRosette` at Angle 0; user rosettes
  never overwritten), foot shell carrying the `SeamCompositeLaminateFP`
  **reused unchanged** (Master=panel, Attachment=web shell,
  SeamRegion=foot shell), `StackAttachmentOverMaster` at creation.
  Pitch scales to foot width / web height (`_scaled_pitch`).
- Weave exclusivity: panel re-supported on the remainder, pre-stiffener
  geometry captured in `SupportBase` driving every recompute; filters
  hidden in composite mode (ADR-0002), restored on mode switch down.
- Graceful degradation: profile without a base edge drops foot machinery
  (hidden + laminate released); regaining a base edge rebuilds.
- Tree claiming via headless `stiffener_claimed_children`.
- Example `stiffener_composite_shell.py` (Z on plate, 30° both sides);
  GUI-verified (shader, exclusivity notch, rosettes above weaves).

## Solver overhaul — the model to preserve

`TransferRosetteFP._solve` is a **closed-form phase-1 frame solve**:

- Both sides measured from rosette frames (master rosette vs the
  transfer's own LCS) — never from drape fields. Drape-lattice boundary
  rows are boundary-snapped and made the residual step-quantised: no
  root-finder can meet it (the old bracket exit masked this).
- Per-sample residuals **fold mod π** (undirected warp; the 180°
  anti-parallel case sat on the atan2 branch cut).
- The residual is exact-linear in the angle (slope −π/180 per degree:
  `Angle` rotates the frame about the very normal the measurement uses);
  the root is one step away, **wrapped** into the principal period via
  `wrap_angle` (never clamped — fabric symmetry).
- `solve_rosette_angle` (AlignFibre, texture-coordinate residual) is a
  probe-seeded secant with the same wrap.

Do not reintroduce per-iteration re-draping of the attachment shell.

## Known-issues register — state after this session

Resolved with evidence: **#1, #2, #4, #5, #6, #7, #8, #9 (code parts)**.
Open: **#3 residual** (closed-surface wrap-around marching — needs
periodic-parameter-aware lattice closing in nextdrape C++), **#9
remainder** (MCP screenshot GL segfault — tooling), and the **unpinned
fine-pitch `solver_failure` flake** (needs a FreeCAD-scale repro; the
nextdrape pitch-sweep tests do not reproduce it).

Notable root causes worth remembering:

- **Black weave = missing shader assets in the build tree.** The
  `CompositesScripts` glob lacked `*.glsl`/`*.png`; the GUI loads Python
  and shader assets from `build/debug/Mod/Composites`, the pixi env
  (headless tests) had them via install — so the suites never caught it.
  Fixed in both globs; verified by stash-bisect that no code change was
  involved.
- **"Stale Shape" was placement stripping.** `fp.Shape = support.Shape`
  stores geometry in its own coordinates and reads back with the
  receiver's (identity) placement. Any mirror of a *placed* support
  shape must sync `Placement` too. `live_support_shape(shell)` in
  `util/geometry_util.py` is the canonical geometry query.

## Test status

- FreeCAD suites (composite shell, seam ×2, stiffener ×2, transfer ×2,
  align, rosette types/scenarios, examples, g7): **all green**, ~110–130
  tests depending on the selection, full affected set ≈ 2 min.
- nextdrape suite: new `CoverageGeometry.FlatPanelAngledSeedStrainClean`
  passes; the previously-undercounted pre-existing failures are recorded
  in known-issues #4 (BaselineShapes ×3, PartLineOutlineTangent,
  EquatorDiscriminator ×4, mould abort) — verified pre-existing by
  stash-bisect on clean `3e97ba0`.

## Environment / workflow facts (new or re-confirmed)

- **New files need a CMake reconfigure** to enter `CompositesScripts`
  (content edits only need the build). `touch CMakeLists.txt` or run the
  build script after adding files; the build script's auto-reconfigure
  does not trigger on new files.
- `compositestests/run_one.py` — run one test case with the log retained
  (`COMPOSITES_TEST_ID=... FreeCADCmd -c "exec(open(...).read())"`);
  `run-tests.sh` deletes its log on success, which hides PROFILER
  output when profiling a single test.
- `Console.PrintMessage` goes to `/tmp/freecad.log`, not the MCP tool
  output; `print()` goes to the tool output. When probing from MCP,
  prefer `print` (or check the log).
- FeaturePython gotchas confirmed this session: a bare Shape assignment
  drops the shape's placement (sync `Placement` too); a recompute may
  clear `Touched` without calling Python execute when the object was
  left touched mid-sweep by another object — poll state, not presence.
- `App::PropertyLinkHidden` is the sanctioned way to hold a reference
  that must not join the DAG (solve references, back-references).

## Suggested next steps

1. ⛔ **Wrap-around lattice closing (nextdrape C++)** — **DO NOT ATTEMPT
   without explicit user approval.** An attempt was already made and
   failed hard; the design analysis sketch in known-issues #3 is NOT a
   validated plan. This item is deliberately placed LAST and is blocked
   pending user sign-off before any new attempt.
2. ✅ **Fine-pitch `solver_failure` flake** — RESOLVED (2026-09). Not
   solver nondeterminism (50 deterministic engine-level solves) but a
   FreeCAD-side bad-seed transient: a shell re-draped mid transfer
   wiring with an unplaced LCS, seeding at the origin. Fixed via seed
   validation (off-surface frame → COM-projection fallback) and a
   deferred re-wiring recompute; see known-issues #11 and the repro
   tool `compositestests/inspect_fine_pitch_flake.py`.
3. ✅ **Compose multiple stiffeners on one panel** — DONE (2026-09).
   Sequential remainders verified and pinned: the remainders chain
   through ``SupportBase``; ``_resupport_panel`` now moves the panel's
   Support pointer only at wiring time (an earlier stiffener's re-wire
   could previously steal the pointer to its shallower remainder,
   resurrecting a later stiffener's seat into the panel weave).
   Acceptance: ``TestMultipleStiffenersOnOnePanel`` (5 tests). Known
   follow-up: deleting a mid-chain stiffener leaves an orphaned
   remainder in the later stiffener's chain (stale until recompute).
4. AlignFibre command: consider a task panel (select shell, anchor,
   second point) now that the wiring is automatic.
